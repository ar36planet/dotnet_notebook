---
title: 02 型別設計
tags: [csharp, class, record, interface, property]
---

# 02 型別設計：class、record、struct 與 properties

## 學習目標

- 從 Java class / field / getter-setter 的思維轉讀 C# type design。
- 分辨 `class`、`record class`、`record struct`、`struct` 的語意。
- 看懂 `get`、`set`、`init`、`required`、`readonly`、`const`、`sealed` 與 object initializer。

## 1. 一句話理解

這一章分的是身分從哪裡來。有些東西靠內容認人，內容一樣就算同一個，用 `record`；有些東西靠自己的身分認人，內容只是它現在的狀態，用 `class`。property 則決定這些狀態什麼時候能被誰改。

## 2. Java 對照

### field 與 constructor injection

Java：

```java
private final UserService userService;

public UserController(UserService userService) {
    this.userService = userService;
}
```

C#：

```csharp
private readonly IUserService _userService;

public UserController(IUserService userService)
{
    _userService = userService;
}
```

`final` 與 `readonly` 的核心關係是：欄位只能在宣告處或 constructor 初始化，不能在之後重新指向另一個物件。它不代表被指向的 object 一定不可變。

C# 12 起也可以把 constructor 參數放到 class 宣告上：

```csharp
public sealed class UserController(IUserService userService)
{
    private readonly IUserService _userService = userService;
}
```

這是 class 的 primary constructor；參數不會像 positional record 那樣自動產生 property，只有在成員使用它時才會被編譯器捕捉成必要的私有狀態。

### Java bean vs C# property

Java 常見：

```java
private String name;

public String getName() { return name; }
public void setName(String name) { this.name = name; }
```

C#：

```csharp
public string Name { get; set; } = string.Empty;
```

C# property 不是單純 public field；它可有 getter / setter 的不同 accessibility，也可放 validation 或計算邏輯。

Java 16 起也有 `record`：

```java
public record Money(BigDecimal amount, String currency) {}
```

對應的 C# 寫法是：

```csharp
public record Money(decimal Amount, string Currency);
```

兩者都適合表達以內容判定相等的資料，但 Java record component 會產生同名 accessor（`amount()`），欄位是 `private final`，record class 也隱含 `final`；C# positional record 會產生 property，且可以繼承另一個 record，`record struct` 還是 value type。

## 3. C# 語法

### class、interface、abstract、sealed

```csharp
public interface IUserRepository
{
    Task<User?> FindByIdAsync(Guid id, CancellationToken cancellationToken);
}

public abstract class Entity
{
    public Guid Id { get; init; }
}

public sealed class Order : Entity
{
    // sealed class 不能再被繼承。
}
```

- `interface`：contract；class 或 struct 可以實作多個 interface。
- `abstract`：不能直接 new，通常提供共用實作或要求 derived class 實作 abstract member。
- `sealed`：停止 inheritance；常用於不希望被 override、或單純表示完整 concrete implementation 的 service。

### properties：`get`、`set`、`init`

```csharp
public sealed class User
{
    public Guid Id { get; init; }
    public string Name { get; set; } = string.Empty;
    public DateTimeOffset CreatedAt { get; init; }
    public bool IsActive { get; private set; } = true;

    public void Deactivate() => IsActive = false;
}

var user = new User
{
    Id = Guid.NewGuid(),
    Name = "Ada",
    CreatedAt = DateTimeOffset.UtcNow
};

// user.Id = Guid.NewGuid(); // init-only，初始化完成後不能再設。
user.Name = "Ada Lovelace"; // set，可在建立後修改。
// user.IsActive = false; // private set，只能在 User 內修改。
user.Deactivate();
```

```csharp
public sealed record UserDto(Guid Id, string Name, DateTimeOffset CreatedAt);
```

`init` 允許 constructor、object initializer 或 `with` 表達式設值，但 object 建立完成後不可再設；它很適合 DTO、設定資料與近似不可變的資料。

`required` 是 C# 11 的 required member。它補上 `init` 沒有提供的保證：呼叫端一定要在 object initializer 或 constructor 中設定該成員，否則編譯器會報 CS9035。`init` 限制的是「何時能設」，`required` 限制的是「一定要設」。

C# 14 起，property accessor 可以用 `field` 直接存取編譯器產生的 backing field：

```csharp
public string RecipientName
{
    get;
    set
    {
        var trimmed = value.Trim();
        field = trimmed.Length > 0
            ? trimmed
            : throw new ArgumentException("收件人不可為空白。", nameof(value));
    }
} = string.Empty;
```

只需要簡單的 getter／setter 時，仍使用 auto-property；需要驗證時，`field` 可以省掉手寫 backing field。

### record、record class、record struct

record 和 class 的差別在相等怎麼判定，對應到 DDD 就是值物件與實體。

DDD 把東西分兩種。一種靠內容認人：兩個人各自寫下 `#FF0000`，講的是同一個紅色，沒有「哪一個」紅色的問題。這種叫值物件（value object）。

另一種靠身分認人：一個杯子有「顏色」這個屬性，今天是紅的，重新噴漆之後變藍的，它還是同一個杯子；而兩個都是紅色的杯子，仍然是兩個杯子。這種叫實體（entity）。

`record` 寫值物件，`class` 寫實體。

```csharp
// 值物件：內容就是身分
public record Money(decimal Amount, string Currency);
public record Address(string City, string Street, string ZipCode);

// 實體：身分是它自己
public sealed class Order
{
    public Guid Id { get; init; }
    public required Address ShippingAddress { get; set; }
    public required Money Total { get; init; }
}
```

客戶打電話來改地址，要判斷地址有沒有真的變，決定要不要重算運費。

```csharp
var before = order.ShippingAddress;
order.ShippingAddress = new Address("台北市", "信義路五段 7 號", "110");

if (before != order.ShippingAddress)
{
    // 重算運費
}
```

`Address` 是 record 的時候這段是對的，比的是三個欄位的內容；客戶把一模一樣的地址重填一次，`before != order.ShippingAddress` 得到 `False`，運費不會被動到。

把 `Address` 換成 class，同一行永遠成立——這裡的前提是沒有另外覆寫 `Equals` 或 `==`。兩次 `new` 就是兩個物件，內容一不一樣它不看；運費每填一次就重算一次。這種 bug 不會噴錯誤，只會默默算錯。

反過來，訂單就算內容一模一樣也是兩張訂單：

```csharp
Console.WriteLine(order == order2); // False，Order 是 class，比的是同不同一個物件
```

```csharp
public record Address(string City, string Street, string ZipCode);
public record class AddressExplicit(string City, string Street, string ZipCode); // 完全同義

public record struct Quantity(int Value);
public readonly record struct Coordinates(double Latitude, double Longitude);
```

`record` 就是 `record class` 的縮寫，骨子裡還是參考型別。內容相等是編譯器幫忙產生 `Equals`、`GetHashCode`、`==`、`!=`、`ToString` 與 positional record 的 `Deconstruct`；它沒有把型別變成 value type。

`record struct` 才是值型別，適合真的很小的值物件。同樣是建構式那排參數，`record class` 生出來的 property 只能在建立時設定，`record struct` 生出來的卻是隨時能改的 `get; set;`。要它不可變，就寫 `readonly record struct`。

`with` 是淺複製。record 裡包一個 `List<string>`，`with` 出來的新物件和舊的共用同一個 list，改一邊兩邊都會動。

EF Core 的 entity 不要為了短就改成 record。entity 要的是身分和變更追蹤，record 給的是內容相等，兩邊要的東西相反。值物件在 EF Core 對應的是 complex type（EF Core 8 起，需用 `[ComplexType]` 或 `ComplexProperty()` 明確設定），不是自己一張表的 entity。EF Core 7 以前常用 owned entity，但它仍有隱藏的 key，不是真正的值語意。

### struct

```csharp
public readonly struct Temperature
{
    public Temperature(double celsius) => Celsius = celsius;

    public double Celsius { get; }
    public double Fahrenheit => Celsius * 9 / 5 + 32;
}
```

`struct` 是 value type。它可以有 method、property、constructor，也可以實作 interface，但不能繼承另一個 class / struct。用在小型、值相等有意義、主要承載資料的 value object；大而可變的 struct 會因複製成本與 mutation 造成困惑。

### `const` vs `static readonly`

```csharp
public static class RetryPolicy
{
    public const int MaxAttempts = 3; // compile-time constant
    public static readonly TimeSpan DefaultTimeout = TimeSpan.FromSeconds(10);
}
```

`const` 只能放編譯期可決定的 primitive / string 等 constant，引用它的 assembly 可能把值 inline。需要 runtime 建立、或不希望被 caller 編譯時嵌入的值，用 `static readonly`。

## 4. 實務範例：domain entity 與 API DTO 分開

```csharp
public sealed class User
{
    public Guid Id { get; init; }
    public required string Name { get; set; }
    public string? PhoneNumber { get; set; }
    public DateTimeOffset CreatedAt { get; init; }
    public bool IsActive { get; private set; } = true;

    public void Deactivate() => IsActive = false;
}

public sealed record UserResponse(
    Guid Id,
    string Name,
    DateTimeOffset CreatedAt);
```

Entity 可以有行為、tracking state、private setter；response DTO 只表達 API contract。不要把 EF entity 直接當 public response，否則資料庫欄位、navigation property 或內部欄位可能意外暴露。

## 5. 常見誤解

- positional `record class` 的參數會產生 `init` property；class 的 primary constructor 參數不會自動產生 property。
- `readonly` field 只阻止「重新指定欄位」，不會凍結可變物件：`readonly List<int>` 仍可 `Add`。
- auto-property 不等於 field；編譯器會產生 backing field，外部只看到 accessor 契約。C# 14 起 accessor 內可用 `field` 讀寫這個 backing field。
- `init` 不是 Java `final` 的完全等價物；它限制 property assignment 的時機，物件內部仍可能有可變狀態。
- `sealed` 不代表 thread-safe，也不代表不可變。
- `struct` 不是「比較快的 class」；它有複製語意，使用前先確認值語意。

## 6. 面試怎麼回答

> 我的判斷點是身分從哪裡來：兩個內容相同的東西該算同一個，那是值物件，用 record class——金額、地址，還有 DTO / command / result 都屬於這類；東西本身有身分、狀態會變、要被 EF Core tracking，那是實體，用 class。`record struct` 是小型 value type。`init` 允許初始化時設定、之後不能再改；`set` 則允許物件建立後修改。`readonly` field 只保證欄位 reference 不會被重新指定，不會讓被指向的物件 immutable。

## 7. 小練習

1. 把一個 Java `record` 改寫成 C# `record`，並說明兩者的 accessor 與繼承差異。
2. 寫一個 `Money` `readonly record struct`，包含 `Amount` 與 `Currency`。
3. 判斷 `readonly List<string> items` 是否可以 `items.Add("x")`，並說明原因。
4. 把上面的 `Address` 從 `record` 改成 `class`，重跑一次「地址有沒有變」的判斷，說明為什麼運費會被重算。
