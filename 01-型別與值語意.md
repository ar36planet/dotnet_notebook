---
title: 01 型別與值語意
tags: [csharp, types, nullable, datetime]
---

# 01 型別與值語意

## 學習目標

- 用 Java 的 mental model 讀懂 C# 常見 built-in 與 BCL type。
- 分清楚 value type、reference type、boxing 與 nullable 的不同層次。
- 在 API / database code 中正確選擇 `DateTime` 或 `DateTimeOffset`。

## 1. 一句話理解

從 Java 過來要重新校準的是三件事：傳出去的是值的複製還是同一個物件、`null` 算不算型別的一部分、存下來的時間是不是全世界都能比對的同一個瞬間。這章的坑幾乎都落在這三件事上，跟 `int` 或 `string` 本身沒什麼關係。

## 2. Java 對照

| C# | Java 大致對應 | 轉換時要注意 |
| --- | --- | --- |
| `string` | `String` | C# 是 reference type，但內容不可變；`==` 比較字串內容 |
| `int`, `long`, `bool` | `int`, `long`, `boolean` | C# numeric / `bool` 是 value type，不是 nullable reference |
| `Guid` | `UUID` | 常作資料庫與 API identifier；`Guid?` 表示可無值 |
| `Uri` | `URI` / `URL` 類型 | C# `Uri` 是 BCL reference type，不是純字串 |
| `DateTime` | `LocalDateTime` 或較弱的 date-time value | 是否帶 UTC / local kind 容易出錯 |
| `DateTimeOffset` | `OffsetDateTime` | 帶「當時的 offset」，適合傳輸與記錄事件時間 |
| `object` | `Object` | C# value type 放入 `object` 會 boxing |
| `T?`（value type） | `Optional<T>` 的語意較接近 | 實際上是 `Nullable<T>`，不是 reference |
| `string?` | `@Nullable String` / Kotlin nullable 類似 | C# 主要是 compiler nullability analysis，不改變 runtime type |

## 3. C# 語法

### 常見型別與 `var`

```csharp
Guid userId = Guid.NewGuid();
Uri callback = new("https://example.com/callback");
int retryCount = 3;
long bytes = 4_000_000L;
bool isActive = true;
string name = "Ada";

// var 仍然是靜態型別；編譯器根據右側推導，不是 dynamic。
var users = new List<string> { "Ada", "Grace" };
var count = users.Count; // int
```

`var` 不會讓變數變成「執行時才決定型別」。以下程式在編譯期就固定為 `int`：

```csharp
var count = 1;
// count = "one"; // 編譯錯誤
```

### value type vs reference type

差別在「傳出去的時候複製了什麼」。這不是拿來背定義的，是會咬人的。

```csharp
public record OrderLine(string Sku, int Quantity);

static void RemoveFirst(List<OrderLine> lines) => lines.RemoveAt(0);

var lines = new List<OrderLine> { new("A-1", 2), new("B-7", 1) };
RemoveFirst(lines);

Console.WriteLine(lines.Count); // 1，呼叫端的 list 真的少了一筆
```

`List<T>` 是 class，參數拿到的是指向同一個 list 的另一支遙控器，按下去動的是同一台電視。method 裡面改了內容，呼叫端看得到。

`int` 就不會：

```csharp
static void AddOne(int count) => count++;

var count = 3;
AddOne(count);

Console.WriteLine(count); // 3，沒被動到
```

還有一個容易混的：在 method 裡面把參數整個換掉，呼叫端不受影響。那是換掉自己手上那支遙控器，電視沒動。

```csharp
static void Replace(List<OrderLine> lines) => lines = [];

var kept = new List<OrderLine> { new("A-1", 2) };
Replace(kept);

Console.WriteLine(kept.Count); // 1，還在
```

常見 value type：數字、`bool`、`enum`、`struct`、`record struct`。常見 reference type：`class`、array、delegate、`string`、`record class`。

### boxing / unboxing

把 value type 塞進 `object`，執行期會在 heap 上另外配一塊記憶體包住它，這叫 boxing；取回來要轉型，叫 unboxing。

```csharp
int number = 42;
object boxed = number;     // boxing：另外配一塊 heap 記憶體
int restored = (int)boxed; // unboxing：必須轉回原本的型別
```

單看一行沒感覺，量大才有。最常踩到的是 log，參數型別是 `object`：

```csharp
// orderId 和 amount 是 value type，每呼叫一次就各包一次
logger.LogInformation("Order {OrderId} paid {Amount}", orderId, amount);
```

一秒幾千筆的 API，這些短命物件會一路餵給 GC。同理，collection 要用 `List<int>` 這種泛型版本，別用 `ArrayList`：`List<int>` 底層直接存 `int`，不會每筆都包一層。

### `DateTime` vs `DateTimeOffset`

台灣是 UTC+8，這段是我最容易出事的地方。用 `DateTime.Now` 把晚上八點存進資料庫，另一端（雲端主機通常跑 UTC）把它當成 UTC 解讀、再換算回台北時間，畫面上就變成隔天凌晨四點。月底最後幾筆訂單會被算到下個月。這種錯不噴 exception，只有數字是錯的。

```csharp
DateTime localOrUtc = DateTime.UtcNow;
DateTimeOffset eventTime = DateTimeOffset.UtcNow;

Console.WriteLine(localOrUtc.Kind);  // Utc
Console.WriteLine(eventTime.Offset); // 00:00:00
```

三個概念要拆開：

- UTC：全球共同的時間基準。
- offset：某個時間表示相對 UTC 快或慢幾小時，例如 `+08:00`。
- time zone：有名稱與規則的地區，例如 `Asia/Taipei`、`America/New_York`，還可能包含 daylight saving time。`DateTimeOffset` 只保存 offset，不保存完整的 time-zone rules。

`DateTime` 有 `Kind`：`Utc`、`Local`、`Unspecified`。`Unspecified` 不是「自動幫你猜」，而是「沒有指定」。跨服務傳輸或寫入資料庫時，含糊的 `DateTime` 很容易造成時區誤讀。

Java 的大致對照：

| Java time | C# / .NET 大致概念 |
| --- | --- |
| `LocalDateTime` | `DateTime` 且沒有可靠的 UTC / local 語意；必要時使用 `DateTimeKind.Unspecified`，但必須由 domain 定義 |
| `OffsetDateTime` | `DateTimeOffset` |
| `ZonedDateTime` | `DateTimeOffset` + `TimeZoneInfo`；若要更豐富的 domain time API，可另評估 Noda Time |
| `Instant` | 常以 UTC 的 `DateTimeOffset` 表達；部分 API 也用 `DateTime` with `Kind=Utc` |

API / event log / audit 欄位通常優先 `DateTimeOffset`：它保留當時 offset，排序與跨系統傳遞較不容易誤會。

```csharp
public sealed record AuditEntry(
    Guid Id,
    string Action,
    DateTimeOffset OccurredAt);
```

若只代表「每天 09:00 的營業時間」這種沒有日期時區瞬間的 domain value，反而不要硬塞成 `DateTimeOffset`；應明確表示它是 local schedule。

### nullable value type

```csharp
int? retryAfterSeconds = null;      // Nullable<int>
Guid? deletedBy = null;             // Nullable<Guid>
DateTimeOffset? completedAt = null; // Nullable<DateTimeOffset>

int seconds = retryAfterSeconds ?? 30;
Guid id = deletedBy ?? Guid.Empty;
```

`int?` 是 `Nullable<int>` 的語法糖。value type 本來不能是 `null`，`Nullable<T>` 用一個 `HasValue` + `Value` 包裝它。

```csharp
if (completedAt is DateTimeOffset time)
{
    Console.WriteLine(time);
}
```

### nullable reference type

```csharp
#nullable enable

string name = "Ada"; // 設計上不允許 null
string? nickname = null; // 設計上允許 null

Console.WriteLine(nickname?.ToUpperInvariant()); // null-safe access
string displayName = nickname ?? name;           // null-coalescing

// ! 只告訴 compiler「我知道這裡不會是 null」；不會在 runtime 做檢查。
Console.WriteLine(nickname!.Length);
```

nullable reference types 是編譯器的 annotations + null-state analysis。它不會把 `string` 換成另一個 runtime class，也不會防止 runtime 的 `NullReferenceException`；它的價值是讓 API contract 與警告更早暴露。

## 4. 實務範例：`null` 是「沒傳」還是「要清空」

```csharp
public sealed record UpdateUserRequest(
    string Name,
    string? PhoneNumber,
    DateTimeOffset? DeactivatedAt);
```

前端送 PATCH 過來，`PhoneNumber` 是 `null`。這代表什麼？

- 使用者沒動這個欄位，你應該保持原值。
- 使用者按了清除，你應該把電話清掉。

`string?` 只講了「這裡可以是 null」，沒講是哪一種。兩種情況在 request 上長得一模一樣，你在 service 裡分不出來，結果不是沒改到，就是把人家的電話清光。

要分得出來，「有沒有出現」就得變成資料的一部分。幾種做法：只更新 request 裡實際出現的欄位（`JsonPatchDocument` 走的是這條）、把 PATCH 拆成語意明確的 endpoint（`POST /users/{id}/phone` 和 `DELETE /users/{id}/phone`），或自己包一個帶「有沒有被設定」旗標的型別。

`string?` 的價值是把「可以沒有」寫進 contract 讓 compiler 幫你盯，不是讓 model binding 自動變安全。真正的輸入驗證還是得靠 validation、domain rule 或資料庫 constraint。

## 5. 常見誤解

- `var` 只是叫 compiler 自己推型別，推完就固定了，跟 JavaScript 的 dynamic 沒有關係。
- `string?` 不是 `Nullable<string>`；reference type 的 nullable 主要是 compiler contract。
- `DateTimeOffset` 只帶 offset，沒有時區規則；要處理日光節約時間得配 `TimeZoneInfo`。
- `DateTime.Now` 不應無腦拿來代表資料庫事件時間；跨主機、跨服務通常用 UTC。
- value type 不等於一定在 stack；boxing、閉包、欄位位置等會影響實際配置。先掌握複製語意，比背 stack / heap 規則重要。
- `!` 只是叫 compiler 這次閉嘴，runtime 什麼檢查都沒做。

## 6. 面試怎麼回答

> `DateTime` 主要表示日期與時間，但它的 `Kind` 可能是 UTC、Local 或 Unspecified，跨系統使用時容易產生歧義。`DateTimeOffset` 會同時保存日期時間與相對 UTC 的 offset，對 API、audit log、事件時間通常更清楚。若需要完整時區規則，還要搭配 `TimeZoneInfo`；它本身不是 time zone。

## 7. 小練習

1. 將 Java 的 `Optional<UUID> deletedBy` 寫成 C#。
2. 你要儲存付款完成時間，選 `DateTime` 還是 `DateTimeOffset`？寫出理由。
3. 解釋這三行各自的角色：`int? x`、`string? s`、`s!`。
