---
title: 17 學習分級與路線圖
tags: [learning-plan, csharp, dotnet, aspnet-core]
---

# 17 學習分級與路線圖

## 立即一定要會

| 主題 | 你應達到的程度 |
| --- | --- |
| nullable | 能看懂 `T?`、`??`、`?.`、`!`，知道哪些是 compiler contract、哪些是 runtime behavior |
| class / record / property | 能判斷 entity、DTO、command、value object 適合哪一種 |
| interface collection | 能為 method 選 `IEnumerable` / `IReadOnlyCollection` / `IReadOnlyList` / `List` |
| LINQ | 熟悉 `Where`、`Select`、`SelectMany`、排序、分組、`Any`、`First`、`Single`、materialization |
| `IEnumerable` / `IQueryable` | 能指出 query 在 memory 還是 SQL，知道 `ToListAsync` 的 boundary |
| async / await | 不寫 `.Result` / `.Wait()`，能閱讀 `Task<T>` 與平行 I/O |
| cancellation | token 從 controller 傳到 EF Core / HttpClient / stream |
| DI lifetimes | 能註冊、注入，避開 singleton → scoped |
| `HttpClientFactory` | 能寫 typed client，處理 timeout、status、JSON、cancellation |
| DTO / JSON | 能說明 model binding、serialization 與 entity 隔離 |
| middleware / Program.cs | 能讀懂 startup、pipeline 與 endpoint mapping |
| integration test | 能用 `WebApplicationFactory` 測 API contract |

## 知道即可

- boxing / unboxing 的存在與常見效能含意，不必先研究 CLR IL。
- `ValueTask<T>` 的使用條件；沒有 profiling 不要主動到處替換 `Task<T>`。
- `IAsyncEnumerable<T>`、`await foreach`，知道它是 async streaming，但先熟悉 `Task<IReadOnlyList<T>>`。
- expression tree 的基本概念與不能任意呼叫 local method 的原因。
- `IOptions<T>`、configuration provider 與 secret management 的基本路線。
- `IAsyncDisposable` 與完整 dispose pattern；先會正確使用 `using`，再深入實作 base class pattern。
- `System.Text.Json` source generation；API correctness 優先，真正有 startup / AOT / throughput 需求再導入。
- `record struct` 與 custom value object 的 copy semantics。
- exception middleware / ProblemDetails 的集中式 mapping。

## 之後再學

- C# 14 extension blocks、field-backed properties、進階 lambda modifiers。
- `Span<T>` / `ReadOnlySpan<T>` / `Memory<T>` 與 zero-copy parsing。
- Native AOT、trimming、source-generated serializers 的全套限制。
- Roslyn analyzer、incremental source generator。
- EF Core compiled queries、interceptors、advanced change tracking、query filters。
- Channels、TPL Dataflow、parallel processing 與 backpressure。
- Polly / resilience pipeline、distributed tracing、OpenTelemetry。
- gRPC、SignalR、Minimal API advanced endpoint filters。
- performance counters、BenchmarkDotNet、allocation profiling。

## 6 週建議路線

| 週次 | 主題 | 產出 |
| --- | --- | --- |
| 1 | [[01-型別與值語意]]、[[02-型別設計]] | 把一組 Java DTO / entity 改寫成 C#，寫出時間與 nullable 決策 |
| 2 | [[03-Generics與Collections]]、[[04-LINQ]] | 用 LINQ 實作 filter、projection、grouping、dictionary lookup |
| 3 | [[05-IEnumerable與IQueryable]]、[[06-非同步程式設計]] | 用 EF query + `ToListAsync`，移除 sync-over-async |
| 4 | [[07-CancellationToken]]、[[08-資源管理]]、[[11-Stream]] | 實作可取消的 file copy / upload |
| 5 | [[09-DependencyInjection]]、[[10-HttpClient]]、[[12-JSON與序列化]] | 做一個 typed client + DTO mapping |
| 6 | [[13-ASP.NET-Core架構]]、[[14-WebApplicationFactory]]、[[15-綜合實作]] | 完成 Users API 與 GET / POST integration tests |

## 完成標準

當你能不用逐行查 syntax，回答以下問題，就達到第一階段目標：

1. `IReadOnlyList<T>` 為什麼不是 `List<T>`？
2. `ToList()` 在 in-memory 與 EF Core query 中各代表什麼？
3. `Task` 為什麼不是 thread？
4. request disconnect 後 token 如何到 DB？
5. 為什麼 `DbContext` 通常 scoped？
6. 為什麼不能每次 request 都 `new HttpClient()`？
7. 為什麼 API 回 DTO 不直接回 entity？
8. `record`、`class`、`record struct` 代表哪些 equality / copy 語意？
9. `Program.cs` 怎麼把 DI 與 middleware 組起來？
10. 如何透過 `WebApplicationFactory` 測一個真實 HTTP endpoint？
