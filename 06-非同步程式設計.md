---
title: 06 非同步程式設計
tags: [csharp, async, await, task]
---

# 06 非同步程式設計：`Task`、`async`、`await`

## 學習目標

- 讀懂 `Task<T>`、`async` 與 `await` 的責任分工。
- 清楚知道 `Task` 不是 thread。
- 在 ASP.NET Core 中避免 blocking、sync-over-async 與錯誤的 async 簽章。

## 1. 一句話理解

`Task<T>` 表示一個「未來會完成並產生 `T` 的非同步操作」；`async/await` 讓程式以近似同步的控制流程等待 I/O，不必把 request 執行緒卡在資料庫或 HTTP response 上。

## 2. Java 對照

| C# | Java | 備註 |
| --- | --- | --- |
| `Task<T>` | `CompletableFuture<T>` | 都表示尚未完成或已完成的非同步結果 |
| `await task` | `thenCompose`／`join` | `await` 不阻塞目前執行緒；`join()` 會等待並包裝例外 |
| `Task.WhenAll` | `CompletableFuture.allOf` | 等待多個非同步操作完成 |
| `.Result`／`.Wait()` | `get()`／`join()` | 都是同步等待；在 request code 會占住執行緒 |

Java 21 的虛擬執行緒讓「同步寫法、阻塞執行緒」的成本降低；.NET 沒有相同的虛擬執行緒模型，靠 `await` 把等待 I/O 的執行緒還給 thread pool，所以 .NET 應用程式碼不應用 `.Result`／`.Wait()`。

## 3. C# 語法

### 先看會出事的地方

把非同步查詢改成同步等待，request 會在資料庫完成前占住 thread：

```csharp
// 不要在 ASP.NET Core request 中這樣寫
var user = _service.GetAsync(id, cancellationToken).Result;
```

正確做法是讓非同步一路傳回呼叫端：

```csharp
var user = await _service.GetAsync(id, cancellationToken);
```

`await` 等待的是一個 `Task`，不是把工作硬塞到另一條 thread。規則：I/O API 使用原生 async 方法，呼叫端一路 `async`／`await`；不要用 `.Result` 或 `.Wait()` 改回同步。

```csharp
public async Task<User> GetUserAsync(
    Guid id,
    CancellationToken cancellationToken)
{
    var user = await _repository.FindByIdAsync(id, cancellationToken);

    return user ?? throw new UserNotFoundException(id);
}

var user = await GetUserAsync(id, cancellationToken);
```

這個簽章的讀法：

- `async`：方法內可以使用 `await`；compiler 會產生 state machine。
- `Task<User>`：呼叫者拿到的是相當於 Java `CompletableFuture<User>` 的非同步結果，不是立即的 `User`。
- `await`：若操作尚未完成，方法暫停並把 continuation 接回 task；完成後再繼續，不需要同步阻塞等待。

### `Task` 不是 thread

`Task` 是工作／結果的抽象；它可能由：

- socket／OS I/O completion 完成，不長時間佔用 managed thread；
- thread pool 上的 CPU 工作完成；
- 已快取的結果直接回傳已完成的 task。

`Task.Run` 是把 CPU-bound 工作丟到 thread pool 的工具，不是用來把 I/O method 包起來的萬用解法。ASP.NET Core 中呼叫 async DB／HTTP API 時，直接 await 原生 async API。

### `ValueTask<T>`

```csharp
public sealed class ExchangeRateCache
{
    private static readonly Dictionary<string, decimal> CachedRates = new()
    {
        ["USD"] = 32.1m,
        ["JPY"] = 0.22m,
    };

    public ValueTask<decimal> GetExchangeRateAsync(
        string currency,
        CancellationToken cancellationToken)
    {
        if (CachedRates.TryGetValue(currency, out var rate))
            return new ValueTask<decimal>(rate); // cache hit：同步完成

        return new ValueTask<decimal>(LoadExchangeRateAsync(currency, cancellationToken));
    }

    private static async Task<decimal> LoadExchangeRateAsync(
        string currency,
        CancellationToken cancellationToken)
    {
        await Task.Delay(TimeSpan.FromMilliseconds(10), cancellationToken);
        return currency == "EUR"
            ? 35.2m
            : throw new KeyNotFoundException($"找不到幣別：{currency}");
    }
}
```

`ValueTask<T>` 適合「高比例同步完成、且呼叫頻率高」的路徑，可能減少 `Task<T>` 配置；但它只能 await 一次，未完成前不可讀 `.Result`，也不可重複呼叫 `AsTask()`。若需要重複 await、傳給 `Task.WhenAll` 或快取，先呼叫一次 `AsTask()` 並保存回傳的 `Task<T>`。應用層服務預設使用 `Task<T>`；只有類別庫或高流量路徑有性能量測證據才考慮 `ValueTask<T>`。

### 併發等待

```csharp
var profileTask = _profileClient.GetAsync(id, cancellationToken);
var ordersTask = _orderRepository.GetRecentAsync(id, cancellationToken);

var allTask = Task.WhenAll(profileTask, ordersTask);
await allTask;

var profile = await profileTask;
var orders = await ordersTask;
```

這種寫法適合兩個互不依賴的 I/O；若對 DB connection pool 或外部 API 造成過載，仍需限制併發。

`Task.WhenAll` 回傳的 task 會保存所有失敗操作的例外；但 `await` 這個 task 時只會直接拋出其中一個。需要完整清單時，先保存回傳值，再從 `allTask.Exception?.InnerExceptions` 讀取所有例外。

## 4. 實務範例：request pipeline 中發生什麼

```csharp
[HttpGet("{id:guid}")]
public async Task<ActionResult<UserDto>> Get(
    Guid id,
    CancellationToken cancellationToken)
{
    var dto = await _service.GetAsync(id, cancellationToken);
    return Ok(dto);
}
```

粗略流程：

```text
Controller action 開始
    ↓ await repository / DB I/O
request thread 不必同步卡住；Task 尚未完成
    ↓ DB 完成，Task 完成
continuation 繼續執行 action
    ↓ 回傳 DTO，ASP.NET Core 序列化 response
```

ASP.NET Core 不會因為 `async` 自動讓 DB 查詢更快；它主要改善在等待 I/O 時 thread 的利用率與可擴充性。

### 例外與 async

```csharp
try
{
    return await _client.GetFromJsonAsync<UserDto>(
        "users/1", cancellationToken)
        ?? throw new InvalidOperationException("Empty response");
}
catch (HttpRequestException ex)
{
    _logger.LogError(ex, "Unable to load user");
    throw;
}
```

在 async 方法中直接 `throw;` 可以保留原始 stack trace。ASP.NET Core 沒有 UI 與舊 ASP.NET 使用的 `SynchronizationContext`，所以 `.Result` 不會造成那類經典死結；但它仍會占住 thread pool 執行緒。應用程式程式碼通常不需要 `ConfigureAwait(false)`；類別庫若不應捕捉呼叫端的 context，才考慮使用它。

## 5. 常見誤解

- `Task` 不是 thread，也不保證新開 thread。
- `async` 不等於平行；只有在不互相依賴的操作同時啟動時才形成併發。
- `Task.Run` 不是 I/O async 的必要步驟。
- 不要在 ASP.NET Core request 程式碼使用 `.Result`、`.Wait()`；它們會占住 thread pool 執行緒，造成 thread pool 耗盡，而且同步等待通常把例外包成 `AggregateException`，`await` 則直接拋出原始例外。
- `async void` 只適合 event handler；呼叫端無法 await，也無法可靠地捕捉例外。controller action 若寫成 `async void`，第一個 `await` 後 request 就可能結束，之後再寫 response 會讓應用程式失敗。應用程式／類別庫方法使用 `Task` 或 `Task<T>`。
- `ValueTask` 不是所有方法的免費效能升級。

## 6. 面試怎麼回答

> `Task<T>` 是代表非同步操作與未來結果的型別，不是 thread。`async/await` 讓方法在等待 I/O 時把控制權還回去，完成後再繼續，因此 ASP.NET Core 可以少占用阻塞執行緒、提高併發量。它不會讓單次資料庫查詢自動變快；也不應用 `.Result` 或 `.Wait()` 把非同步程式碼改成同步。`ValueTask<T>` 只在已完成比例高且經過性能量測的熱路徑使用。

## 7. 小練習

1. 將一個同步 `User GetUser(Guid id)` 改成 async 簽章。
2. 判斷兩個獨立外部 API 是否適合 `Task.WhenAll`。
3. 解釋為什麼 `Task.Run(() => _db.Orders.ToList())` 不是正確的 EF Core async 寫法。
