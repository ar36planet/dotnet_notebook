---
title: 06 非同步程式設計
tags: [csharp, async, await, task]
---

# 06 非同步程式設計：`Task`、`async`、`await`

## 學習目標

- 讀懂 `Task<T>`、`async` 與 `await` 的責任分工。
- 清楚知道 `Task` 不是 thread。
- 在 ASP.NET Core 中避免 blocking、sync-over-async 與錯誤的 async signature。

## 1. 一句話理解

`Task<T>` 表示一個「未來會完成並產生 `T` 的非同步操作」；`async/await` 讓你以近似同步的控制流程等待 I/O，不必把 request thread 卡在資料庫或 HTTP response 上。

## 2. 先看會出事的地方

把非同步查詢改成同步等待，request 會在資料庫完成前占住 thread：

```csharp
// 不要在 ASP.NET Core request 中這樣寫
var user = _service.GetAsync(id, cancellationToken).Result;
```

正確做法是讓非同步一路傳回呼叫端：

```csharp
var user = await _service.GetAsync(id, cancellationToken);
```

`await` 等待的是一個 `Task`，不是把工作硬塞到另一條 thread。真正要先記住的規則是：I/O API 使用原生 async 方法，呼叫端也使用 `async` / `await`；不要用 `.Result` 或 `.Wait()` 把它改回同步。

## 3. C# 語法

```csharp
public async Task<User> GetUserAsync(
    Guid id,
    CancellationToken cancellationToken)
{
    var user = await _repository.FindByIdAsync(id, cancellationToken);

    return user ?? throw new UserNotFoundException(id);
}

var user = await GetUserAsync(id, cancellationToken);
```

這個 signature 的讀法：

- `async`：方法內可以使用 `await`；compiler 會產生 state machine。
- `Task<User>`：呼叫者拿到的是 future-like handle，不是立即的 `User`。
- `await`：若 operation 尚未完成，方法暫停並把 continuation 接回 task；完成後再繼續，不需要同步 blocking 等待。

### `Task` 不是 thread

`Task` 是工作／結果的抽象；它可能由：

- socket / OS I/O completion 完成，不長時間佔用 managed thread；
- thread pool 上的 CPU work 完成；
- 已完成的 cached result 立即完成。

`Task.Run` 是把 CPU-bound work 丟到 thread pool 的工具，不是把所有 I/O method 加上 `Task.Run` 的標準修復方法。ASP.NET Core 中呼叫 async DB / HTTP API 時，直接 await 原生 async API。

### `ValueTask<T>`

```csharp
public ValueTask<int> GetCachedCountAsync()
    => new(42);
```

`ValueTask<T>` 允許某些已同步完成的 hot path 減少配置，但使用規則比 `Task<T>` 複雜：通常只能 await 一次、不可任意 cache / share。application service 預設使用 `Task<T>`；只有 library / high-throughput path 有 profiling 證據才考慮 `ValueTask<T>`。

### 平行等待

```csharp
var profileTask = _profileClient.GetAsync(id, cancellationToken);
var ordersTask = _orderRepository.GetRecentAsync(id, cancellationToken);

await Task.WhenAll(profileTask, ordersTask);

var profile = await profileTask;
var orders = await ordersTask;
```

這種寫法適合兩個 I/O 不互相依賴的工作；不是所有 code 都該平行，若對 DB connection pool 或外部 API 造成過載，仍需限制併發。

## 4. 實務範例：request pipeline 中發生什麼

```csharp
[HttpGet("{id:guid}")]
public async Task<ActionResult<UserDto>> Get(
    Guid id,
    CancellationToken cancellationToken)
{
    var dto = await _service.GetAsync(id, cancellationToken);
    return Ok(dto);
}
```

粗略流程：

```text
Controller action 開始
    ↓ await repository / DB I/O
request thread 不必同步卡住；Task 尚未完成
    ↓ DB 完成，Task 完成
continuation 繼續執行 action
    ↓ 回傳 DTO，ASP.NET Core 序列化 response
```

ASP.NET Core 不會因為 `async` 自動讓 DB 查詢更快；它主要改善在等待 I/O 時 thread 的利用率與可擴充性。

### 例外與 async

```csharp
try
{
    return await _client.GetFromJsonAsync<UserDto>(
        "users/1", cancellationToken)
        ?? throw new InvalidOperationException("Empty response");
}
catch (HttpRequestException ex)
{
    _logger.LogError(ex, "Unable to load user");
    throw;
}
```

在 async method 中通常直接 `throw;` 保留 stack trace；不要用 `.Result` / `.Wait()` 把 async 強行變同步。

## 5. 常見誤解

- `Task` 不是 thread，也不保證新開 thread。
- `async` 不等於平行；只有在不互相依賴的 operations 同時啟動時才形成 concurrency。
- `Task.Run` 不是 I/O async 的必要步驟。
- 不要在 ASP.NET Core request code 使用 `.Result`、`.Wait()`，可能造成 thread starvation，也讓 exception / cancellation 行為更難讀。
- `async void` 只適合 event handler；application / library method 使用 `Task` 或 `Task<T>`。
- `ValueTask` 不是所有方法的免費效能升級。

## 6. 面試怎麼回答

> `Task<T>` 是代表非同步操作與未來結果的型別，不是 thread。`async/await` 讓方法在等待 I/O 時把控制權還回去，完成後再繼續，因此 ASP.NET Core 可以少佔用 blocking thread、提高 concurrency。它不會讓單次 DB query 自動變快；也不應用 `.Result` 或 `.Wait()` 把 async code 變同步。`ValueTask<T>` 只在已完成比例高且經過 profiling 的 hot path 使用。

## 7. 小練習

1. 將一個同步 `User GetUser(Guid id)` 改成 async signature。
2. 判斷兩個獨立外部 API 是否適合 `Task.WhenAll`。
3. 解釋為什麼 `Task.Run(() => _db.Users.ToList())` 通常不是正確的 EF Core async 寫法。
