---
title: 08 資源管理
tags: [csharp, idisposable, using, streams]
---

# 08 資源管理：`IDisposable`、`using` 與 `IAsyncDisposable`

## 學習目標

- 知道 .NET 為什麼需要確定性釋放。
- 讀懂 `using var`、`await using` 背後的 try/finally。
- 知道何時應用程式碼要負責釋放 stream、response、transaction 等資源。

## 1. 一句話理解

GC 只管理受控記憶體，不保證檔案 handle、socket、DB transaction 等外部資源及時釋放；`IDisposable`／`IAsyncDisposable` 為資源擁有者提供確定性的釋放契約。

## 2. Java 對照

| Java | C# | 差異 |
| --- | --- | --- |
| `AutoCloseable.close()` | `IDisposable.Dispose()` | 同步釋放資源 |
| `try (var stream = ...) {}` | `using (var stream = ...) {}`／`using var stream = ...;` | 離開 scope 時釋放；多個資源都按宣告反序釋放 |
| `try-with-resources` | `using`／`await using` | C# 的 `await using` 對應 `IAsyncDisposable`，Java 沒有相同的語法對應 |
| suppressed exception | `finally` 中的釋放例外 | Java 可把 close 例外附在原例外上；C# 的 `Dispose()` 若再丟例外，可能蓋掉原本的例外 |

## 3. C# 語法

### 先看會出事的地方

檔案寫入完成卻沒有釋放 stream，同一個 request 再開啟檔案時會失敗。以下程式刻意保留第一個 `FileStream`：

```csharp
var path = Path.Combine(Path.GetTempPath(), "order-export.csv");
var first = File.Create(path);

try
{
    using var second = File.OpenRead(path);
}
catch (IOException exception)
{
    Console.WriteLine($"{exception.GetType().FullName}: {exception.Message}");
}
finally
{
    first.Dispose();
    File.Delete(path);
}
```

實際輸出（本機 macOS 的暫存路徑會依環境不同）：

```text
System.IO.IOException: The process cannot access the file '/var/folders/bn/gwgp3pkd63l0fhh4pz_ws_rr0000gn/T/order-export.csv' because it is being used by another process.
```

建立資源的 scope 應該擁有它：

```csharp
await using var output = File.Create("order-export.csv");
await using var input = File.OpenRead("orders.csv");

await input.CopyToAsync(output, cancellationToken);
// scope 結束時反序釋放：先 input.DisposeAsync()，再 output.DisposeAsync()
```

`using`／`await using` 展開後就是 `try/finally`，把 `Dispose`／`DisposeAsync` 放在 scope 結束時執行。

## 3. C# 語法

### `IDisposable` 與 using

```csharp
using (var stream = File.OpenRead(path))
{
    // read stream
}

using var anotherStream = File.OpenRead(path);
// 目前 scope 結束時 Dispose()
```

概念上接近：

```csharp
var stream = File.OpenRead(path);
try
{
    // use stream
}
finally
{
    stream.Dispose();
}
```

### `IAsyncDisposable` 與 await using

```csharp
await using var transaction =
    await db.Database.BeginTransactionAsync(cancellationToken);

// async work
await transaction.CommitAsync(cancellationToken);
// scope 結束呼叫 DisposeAsync()
```

`DisposeAsync()` 回傳 `ValueTask`；當釋放本身需要非同步 I/O 時使用。具體例子包括 `DbTransaction`、`IDbContextTransaction`、`FileStream` 與 `NetworkStream`。

### `try/catch/finally`、`throw`、custom exception

以下片段位於可取得 `_repository`、`_logger` 與 `id` 的 service method 中。

```csharp
public sealed class OrderNotFoundException(Guid id)
    : Exception($"Order {id} was not found.");

try
{
    var order = await _repository.FindAsync(id, cancellationToken)
        ?? throw new OrderNotFoundException(id);
}
catch (OrderNotFoundException exception)
{
    _logger.LogDebug(exception, "Order not found: {OrderId}", id);
    throw; // 有記錄動作時，用 throw 保留原始 stack trace
}
finally
{
    // 一定會跑；只放釋放與還原狀態，不放業務邏輯，因為再丟例外會蓋掉原本的例外。
}
```

若只是為了讓例外往上丟，整個 `catch` 可以刪掉；例外本來就會傳到上層。領域例外對應到 HTTP 狀態碼的邏輯集中在 `IExceptionHandler`，不要每個 controller 都複製一套 try/catch。

## 4. 實務範例：上傳或處理檔案

以下 action 由 ASP.NET Core controller 提供 `_storageRoot`。

```csharp
[HttpPost("upload")]
public async Task<IActionResult> Upload(
    IFormFile file,
    CancellationToken cancellationToken)
{
    if (file.Length == 0)
    {
        return BadRequest("Empty file");
    }

    var path = Path.Combine(_storageRoot, Path.GetRandomFileName());
    await using var output = System.IO.File.Create(path);
    await file.CopyToAsync(output, cancellationToken);
    return Ok(new { file.Length });
}
```

`IFormFile` 的 multipart 內容在 action 執行前已由 ASP.NET Core 緩衝到記憶體或暫存檔；`OpenReadStream()` 是讀取這份已緩衝內容，不是從客戶端到儲存區的串流上傳。大型檔案要改用適合的 multipart streaming API，並設定 request 大小上限。

這個 action 建立了 `output`，所以在這裡釋放；`IFormFile` 由框架管理。真實系統還要驗證副檔名、content type、檔案大小、檔名 path traversal、病毒掃描與儲存位置。

### 自訂 disposable type

```csharp
public sealed class TempFile : IDisposable
{
    private readonly string _path;

    public TempFile(string path) => _path = path;

    public void Dispose()
    {
        File.Delete(_path);
    }
}
```

`TempFile` 是 sealed 且沒有 finalizer，`GC.SuppressFinalize` 在這裡沒有作用。需要直接持有 unmanaged handle 時，優先使用 `SafeHandle`；非 sealed class 才需要可覆寫的 `Dispose(bool)` 完整 pattern。應用程式的 disposable 型別多半是 sealed，只負責把持有的 `IDisposable` 依序 Dispose。

## 5. 常見誤解

- `Dispose` 不是釋放受控記憶體；GC 仍由 runtime 管理。
- 拿到的 `IDisposable` 不一定歸呼叫端釋放，先確認擁有權。DI 容器建立的 transient／scoped service 在解析它的 scope 結束時釋放，singleton 在 root provider 釋放時釋放。
- `AddSingleton(new MyService())` 傳入的實例不是 container 建立的，container 不會自動釋放；建立者要自己負責。
- 從 root provider 解析 disposable transient，container 會一直保存它到 root provider 結束，可能造成記憶體持續增加。
- `using var` 的 scope 是目前 block / method，不是整個程式。
- `IAsyncDisposable` 不會自動被同步 `Dispose()` 呼叫；只實作它的 service 若由同步 scope 收尾，會得到：

  ```text
  System.InvalidOperationException: 'AsyncOnly' type only implements IAsyncDisposable. Use DisposeAsync to dispose the container.
  ```

  需要 `await using` 或明確 `DisposeAsync()`。
- 由 DI 注入的 typed client 不要自己 `using`；它是容器的 transient service，handler 的生命週期由 `IHttpClientFactory` 管理，`HttpClient` 本身不需要自行釋放。

## 6. 面試怎麼回答

> `IDisposable` 是同步釋放契約，`IAsyncDisposable` 是非同步釋放契約。`using`／`await using` 會在 scope 結束時以 try/finally 的方式呼叫 `Dispose`／`DisposeAsync`，主要處理檔案、socket、transaction 這類外部資源。是否該釋放還要看資源擁有權。

## 7. 小練習

1. 把一個檔案讀取範例改寫成 `using var`。
2. 判斷 `DbTransaction` 應使用 `using` 還是 `await using`（提示：看它有沒有實作 `IAsyncDisposable`）。
3. 說明為什麼 `finally` 常放釋放工作，而不放主要業務操作。
