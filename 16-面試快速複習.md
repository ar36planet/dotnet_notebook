---
title: 16 面試快速複習 Cheat Sheet
tags: [interview, csharp, dotnet, aspnet-core]
---

# 16 面試快速複習 Cheat Sheet

## 型別與語法

| 問題 | 30 秒回答 |
| --- | --- |
| `var` 是 dynamic 嗎？ | 不是。`var` 由 compiler 根據右側推導成靜態型別；runtime 不會因此失去 type safety。 |
| value type vs reference type？ | value type 賦值通常複製值；reference type 賦值複製 reference。class / array / string 是 reference，numeric / enum / struct 是 value。不要把它簡化成一定在 stack / heap。 |
| `record` vs class？ | record 偏資料與 value equality、`with` copy；class 偏 identity、behavior、mutable state 或 EF entity。`record` 預設是 record class；`record struct` 才是 value type。 |
| `init` vs `set`？ | `init` 只能在 object construction / initializer 階段設定；`set` 允許建立後修改。 |
| `readonly`？ | field 只能在宣告或 constructor 重新指定；不代表被 reference 的 object immutable。 |
| `sealed`？ | 不允許再繼承；表達 class 的 extension boundary，不等於 thread-safe 或 immutable。 |
| `const` vs `static readonly`？ | `const` 是 compile-time constant，可能被 caller inline；`static readonly` 在 runtime 初始化，適合物件或非 compile-time 值。 |

## Nullable 與時間

| 問題 | 30 秒回答 |
| --- | --- |
| `int?`？ | `Nullable<int>`，value type 的可無值包裝，包含 `HasValue` / `Value`。 |
| `string?`？ | nullable reference annotation，搭配 compiler null-state analysis；runtime type 仍是 string reference，不會自動做 runtime null check。 |
| `?.` / `??` / `!`？ | `?.` null-conditional access；`??` null-coalescing fallback；`!` null-forgiving，只壓掉 warning，不會檢查或修正 null。 |
| `DateTime` vs `DateTimeOffset`？ | `DateTime` 帶 Kind，可能是 UTC / Local / Unspecified；`DateTimeOffset` 帶當時 offset，API / audit event 通常更清楚，但它不是完整 time zone。 |

## Collections 與 LINQ

| 問題 | 30 秒回答 |
| --- | --- |
| `IEnumerable<T>` vs `List<T>`？ | `IEnumerable<T>` 只承諾可列舉；`List<T>` 暴露具體 mutable list。public API 優先暴露 caller 需要的最小能力。 |
| `IReadOnlyList<T>` 的價值？ | 呼叫者可列舉、取 count、用 index，但看不到 Add / Remove；不保證底層 object 深層 immutable。 |
| `Where / Select / ToList`？ | `Where` filter、`Select` projection、`ToList` 列舉並 materialize snapshot；後者可能造成 allocation 或觸發 DB query。 |
| `First` vs `Single`？ | `First` 只要求至少一筆；`Single` 要求剛好一筆，0 或多筆都錯。選擇是在表達 domain assumption。 |
| `SelectMany`？ | flatten nested collections，對應 Java Stream 的 `flatMap`。 |

## `IEnumerable` / `IQueryable` / EF Core

| 問題 | 30 秒回答 |
| --- | --- |
| `IQueryable<T>`？ | provider 可解析的 query abstraction；lambda 可能是 expression tree。EF Core 可把它翻成 SQL。 |
| 何時查 DB？ | 通常在 `ToListAsync`、`FirstAsync`、`AnyAsync` 等 terminal operation；在此之前是組 query。 |
| 如何避免效能問題？ | filter / projection / pagination 盡量留在 `IQueryable`；避免過早 materialization、N+1、載入整個 entity 或在 loop 中各查一次。 |
| `AsEnumerable` 的意義？ | 把後續 pipeline 切到 LINQ to Objects；應確認拉到 memory 的資料量。 |

## Async / cancellation / resource

| 問題 | 30 秒回答 |
| --- | --- |
| `Task<T>`？ | 代表非同步 operation 與未來的 `T` 結果，不是 thread。`await` 在等待 I/O 時讓出控制權，完成後再繼續。 |
| `Task.Run` 要不要包所有 async？ | 不要。I/O API 應直接 await；`Task.Run` 主要是把 CPU-bound 工作交給 thread pool。 |
| `CancellationToken`？ | cooperative cancellation notification；由 request boundary 傳到 DB、HttpClient、stream。它不會強制 kill thread。 |
| `RequestAborted`？ | client disconnect 或 server abort 時 signal 的 HTTP request token；可傳進長時間 I/O。 |
| `using`？ | scope 結束時呼叫 `Dispose`，概念上由 try/finally 保證 cleanup。`await using` 呼叫 `DisposeAsync`。 |

## DI / ASP.NET Core

| 問題 | 30 秒回答 |
| --- | --- |
| constructor injection？ | class 透過 constructor 宣告必要 dependencies，由 DI container 在 composition root 組裝；物件建立完成時依賴完整，也容易替換 fake。 |
| Singleton / Scoped / Transient？ | singleton 全 app 一份、scoped 每 request scope 一份、transient 每次 resolve 新建。DbContext 常 scoped；避免 singleton capture scoped。 |
| `Program.cs`？ | 現代 ASP.NET Core 的 host / configuration / DI composition root 與 middleware / endpoint pipeline 定義處。 |
| middleware？ | 依序處理 request 的 chain；可在 next 前後工作，也可短路 response，順序很重要。 |
| DTO 為什麼？ | 隔離 API contract、domain entity 與 persistence schema，避免過度暴露、循環 reference、資料欄位耦合。 |

## HttpClient / testing

| 問題 | 30 秒回答 |
| --- | --- |
| 為什麼不每次 new HttpClient？ | 反覆建立 / dispose 可能造成 connection / socket 問題；IHttpClientFactory 管理 handler lifetime、集中設定與 logging。 |
| typed client？ | 以 class 封裝一個外部 API，透過 AddHttpClient 設定 base address 與 handler；讓外部 API 邊界可測試。 |
| unit vs integration test？ | unit 測單一 class、用 fake / mock；integration 用完整 test host + HTTP 驗證 routing、DI、middleware、JSON、status code。 |
| WebApplicationFactory？ | 建立 in-process TestServer，`CreateClient()` 讓測試像 client 一樣呼叫 API；可用 custom factory 替換 DB / external client。 |

## 目標 code 逐項拆解

```csharp
public sealed class UserService : IUserService
{
    private readonly IUserRepository _repository;

    public UserService(IUserRepository repository)
    {
        _repository = repository;
    }

    public async Task<IReadOnlyList<UserDto>> GetUsersAsync(
        CancellationToken cancellationToken)
    {
        var users = await _repository
            .GetUsersAsync(cancellationToken);

        return users
            .Where(x => x.IsActive)
            .Select(x => new UserDto(
                x.Id,
                x.Name,
                x.CreatedAt))
            .ToList();
    }
}
```

- `sealed`：停止繼承。
- `IUserService`：implementation 遵守的 abstraction。
- `private readonly`：欄位建立後不重新指定。
- constructor injection：DI container 傳入 repository。
- `Task<IReadOnlyList<UserDto>>`：async operation，完成後是一組只讀 view。
- `CancellationToken`：讓 caller / HTTP request 可以取消底層工作。
- `Where`：filter active users。
- `Select`：entity → DTO projection。
- `ToList`：materialize；若 `users` 是 `IQueryable`，用 `ToListAsync` 才是非同步 DB execution boundary。
- DTO：不直接 expose persistence entity。

