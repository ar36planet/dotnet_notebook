---
title: 16b 面試快速複習 Cheat Sheet
tags: [interview, csharp, dotnet, aspnet-core]
---

# 16 面試快速複習 Cheat Sheet

MVC、Razor、Model Binding、Validation、PRG 和 Anti-Forgery 的完整面試題見 [[16-ASP.NET-Core-MVC面試實戰]]；本章保留跨 C#、.NET、ASP.NET Core 和 EF Core 的快速索引。

## 型別與語法

| 問題 | 30 秒回答 |
| --- | --- |
| `var` 是 dynamic 嗎？ | 不是。`var` 由 compiler 根據右側推導成靜態型別；runtime 不會因此失去 type safety。 |
| value type vs reference type？ | value type 賦值複製整個 instance（內含 reference 欄位只複製 reference）；reference type 賦值複製 reference。類別、陣列、字串是參考型別；數值、列舉、struct 是值型別。不要把它簡化成一定在 stack / heap。 |
| `record` vs class？ | record 偏資料與 value equality、`with` copy；class 用在有身分、有行為、狀態會變或要被 EF Core 追蹤的東西。`record` 預設是 record class；`record struct` 才是 value type。 |
| `init` vs `set`？ | `init` 只能在 constructor、object initializer 或 `with` expression 設定；`set` 允許建立後修改。 |
| `readonly`？ | field 只能在宣告或 constructor 重新指定；不代表被 reference 的 object immutable。 |
| `sealed`？ | 不允許再繼承；表達 class 的 extension boundary，不等於 thread-safe 或 immutable。 |
| `const` vs `static readonly`？ | `const` 是 compile-time constant，會被展開進呼叫端組件；改值後呼叫端要重編。`static readonly` 在 runtime 初始化，適合物件或非 compile-time 值。 |

## Nullable 與時間

| 問題 | 30 秒回答 |
| --- | --- |
| `int?`？ | `Nullable<int>`，value type 的可無值包裝，包含 `HasValue` / `Value`。 |
| `string?`？ | 可為 null 的參考型別標註，讓編譯器做 null 狀態分析；runtime type 仍是 string reference，不會自動做 null check。 |
| `?.` / `??` / `!`？ | `?.` null-conditional access；`??` null-coalescing fallback；`!` null-forgiving，只壓掉 warning，不會檢查或修正 null。 |
| `DateTime` vs `DateTimeOffset`？ | `DateTime` 帶 Kind，可能是 UTC / Local / Unspecified；`DateTimeOffset` 帶當時 offset，API / audit event 通常更清楚，但它不是完整 time zone。 |

## Collections 與 LINQ

| 問題 | 30 秒回答 |
| --- | --- |
| `IEnumerable<T>` vs `List<T>`？ | `IEnumerable<T>` 只承諾可列舉；`List<T>` 暴露具體可變清單。public API 優先暴露呼叫端需要的最小能力。 |
| `IReadOnlyList<T>` 的價值？ | 呼叫者可列舉、取 count、用 index，但看不到 Add / Remove；不保證底層 object 深層 immutable。 |
| `Where / Select / ToList`？ | `Where` 篩選、`Select` 投影、`ToList` 列舉並落成一份記憶體清單；它會配置 list，對 `IQueryable` 則是送出 SQL 的時間點。 |
| `First` vs `Single`？ | `First` 只要求至少一筆；`Single` 要求剛好一筆，0 或多筆都錯。選 `Single` 等於宣告資料最多一筆。 |
| `SelectMany`？ | 把巢狀集合展平成一條序列。 |

## `IEnumerable` / `IQueryable` / EF Core

| 問題 | 30 秒回答 |
| --- | --- |
| `IQueryable<T>`？ | provider 能解析的查詢抽象；傳給 `Queryable` operator 的 lambda 會編譯成 `Expression<Func<T, ...>>`，不是 delegate，EF Core 據此翻成 SQL。 |
| 何時查 DB？ | 列舉（`foreach`／`await foreach`）或呼叫 `ToList`／`ToListAsync`、`First`／`FirstAsync`、`Count`、`Any` 等立即執行運算子時才送出 SQL；在此之前只是組 expression tree。 |
| 如何避免效能問題？ | filter／projection／pagination 盡量留在 `IQueryable`；避免太早落地成記憶體集合、N+1、載入整個 entity 或在迴圈中各查一次。 |
| `AsEnumerable` 的意義？ | 把後續 pipeline 切到 LINQ to Objects；應確認拉到 memory 的資料量。 |

## Async / cancellation / resource

| 問題 | 30 秒回答 |
| --- | --- |
| `Task<T>`？ | 代表非同步作業與未來的 `T` 結果，不是 thread。`await` 在 task 尚未完成時讓出控制權給 caller，完成後接續執行；已完成的 task 不會讓出。 |
| `Task.Run` 要不要包所有 async？ | 不要。I/O API 應直接 await；`Task.Run` 主要是把 CPU-bound 工作交給 thread pool。 |
| `CancellationToken`？ | 合作式取消的通知；從 request 進來一路傳到 DB、`HttpClient`、stream。它不會強制終止執行緒。 |
| `RequestAborted`？ | 客戶端斷線、伺服器中止或 request timeout 時 signal 的 HTTP request token；可傳進長時間 I/O。 |
| `using`？ | scope 結束時呼叫 `Dispose`，概念上由 try/finally 保證 cleanup。`await using` 呼叫 `DisposeAsync`。 |
| `ValueTask<T>` 何時用？ | 預設回傳 `Task<T>`；只有高比例同步完成且經過性能量測的熱路徑才用。它只能 await 一次；需要重複使用時先 `AsTask()`。 |

## DI / ASP.NET Core

| 問題 | 30 秒回答 |
| --- | --- |
| constructor injection？ | class 透過 constructor 宣告必要依賴，由 DI container 在組裝點組好；物件建好時依賴就齊了，測試也容易換成假物件。 |
| Singleton / Scoped / Transient？ | singleton 全 app 一份、scoped 每 request scope 一份、transient 每次 resolve 新建。DbContext 常 scoped；避免 singleton capture scoped，singleton 也必須 thread-safe。 |
| `Program.cs`？ | 現代 ASP.NET Core 的 host / configuration / DI composition root 與 middleware / endpoint pipeline 定義處。 |
| middleware？ | 依序處理 request 的鏈；可在呼叫 next 前後做事，也可以直接回應不往下傳，順序很重要。 |
| DTO 為什麼？ | 隔離 API 契約、領域實體與資料庫 schema，避免多暴露欄位、循環參考、欄位耦合。 |

## HttpClient / testing

| 問題 | 30 秒回答 |
| --- | --- |
| 為什麼不每次 new HttpClient？ | 每次建立會各開一個 connection pool，高頻下可能耗盡 port；長壽命 singleton 又不會追 DNS 變更。兩條正解是 static／singleton + `PooledConnectionLifetime`，或 `IHttpClientFactory`（handler 預設 2 分鐘輪換）。 |
| typed client？ | 以 class 封裝一個外部 API，透過 `AddHttpClient` 設定 base address 與 handler；typed client 是 transient，不要注入 singleton。 |
| unit vs integration test？ | 單元測試測一個 class、用假物件；整合測試起完整 test host、走 HTTP，驗證 routing、DI、middleware、JSON、status code。 |
| WebApplicationFactory？ | 建立 in-process TestServer，`CreateClient()` 讓測試像 client 一樣呼叫 API；可用 custom factory 替換 DB／外部 client。 |

## Stream / JSON

| 問題 | 30 秒回答 |
| --- | --- |
| `Stream`？ | 是依序讀寫位元組的抽象；`Read` 可能少讀，固定長度用 `ReadExactly`，大型上傳不要把整檔轉成 `byte[]`。 |
| `System.Text.Json` vs Newtonsoft.Json？ | `System.Text.Json` 是 ASP.NET Core 預設；多型用 .NET 7+ 的 `[JsonPolymorphic]`／`[JsonDerivedType]`，需要 `TypeNameHandling`、JsonPath 或舊 attribute 相容性時才用 Newtonsoft.Json。 |

## 目標 code 逐項拆解

```csharp
public sealed class UserService : IUserService
{
    private readonly IUserRepository _repository;

    public UserService(IUserRepository repository)
    {
        _repository = repository;
    }

    public async Task<IReadOnlyList<UserDto>> GetUsersAsync(
        CancellationToken cancellationToken)
    {
        var users = await _repository
            .GetUsersAsync(cancellationToken);

        return users
            .Where(x => x.IsActive)
            .Select(x => new UserDto(
                x.Id,
                x.Name,
                x.CreatedAt))
            .ToList();
    }
}
```

這段的 repository 回傳已具體化的集合（對應第 15 章的 in-memory sample），所以 `Where` 在記憶體執行；接 EF Core 時，篩選應搬進 repository 的 `IQueryable`，最後用 `ToListAsync(cancellationToken)` 收尾。

- `sealed`：停止繼承。
- `IUserService`：implementation 遵守的 abstraction。
- `private readonly`：欄位建立後不重新指定。
- constructor injection：DI container 傳入 repository。
- `Task<IReadOnlyList<UserDto>>`：async operation，完成後是一組只讀 view。
- `CancellationToken`：讓 caller / HTTP request 可以取消底層工作。
- `Where`：filter active users。
- `Select`：entity → DTO projection。
- `ToList`：把已取得的集合落成清單；若來源是 `IQueryable`，`ToList` 與 `ToListAsync` 都會執行查詢，差別是同步或非同步。
- DTO：不直接 expose persistence entity。
